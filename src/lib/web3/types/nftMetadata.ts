export type nftMetadata = {
    name: string;
    description: string;
    imageUri: string;
    symbol: string;
    family_name: string;
    collection_name: string;
    external_url: string;
    background: string;
};